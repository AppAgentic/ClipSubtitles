{
  "name": "@clipsubtitles/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Typed schemas, error codes, and tool/REST contracts shared by the API, MCP surface, and web editor",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.5.2"
  }
}
