# Parked actions

Only items that require Joe or an external system. Everything else in the
plan is implemented locally and covered by `pnpm check`. Nothing here has been
submitted, published, or provisioned.

| # | Gate | Why it is blocked locally | Safe next action |
|---|------|---------------------------|------------------|
| 1 | **Live transcription benchmark** (select the production provider) | Needs `GEMINI_API_KEY` + `GEMINI_TRANSCRIBE_MODEL`, `ELEVENLABS_API_KEY`, `OPENAI_API_KEY`. The harness, corpus, scorer, and acceptance gates are implemented and validated with mock providers only; the report explicitly refuses to name a winner without live evidence and a `whisper` baseline run. The live adapter request/response mappings are written against public API shapes but are **unverified**. No live provider call was made from this repository. | Inject the keys from `mc-vault` into the process environment for one shell session (never write them to `.env` or the repo), run `pnpm benchmark --providers gemini,elevenlabs,gpt-transcribe,whisper --repeats 2`, review `fixtures/benchmark/reports/latest.md`, then set `TRANSCRIPTION_PROVIDERS=<winner>,<fallback>`. Fix any adapter mapping mismatches surfaced by the first live run. |
| 2 | **WorkOS/AuthKit tenant** (identity + OAuth authority) | No WorkOS environment or client id exists for `clipsubtitles.com`. `AUTH_MODE=workos` is config-gated: the AuthKit code flow, RS256 JWKS verification, and the webhook receiver are implemented but unverified against a live tenant. Event names (`session.revoked`, `user.deleted`) follow the WorkOS catalogue and must be confirmed. | Create the WorkOS environment, an AuthKit client with redirect `https://clipsubtitles.com/auth/callback` (the web origin proxies `/auth/*` to the API so the session cookie lands on the site), enable the Connect/OAuth feature for the MCP resource `https://api.clipsubtitles.com/api/mcp` with scopes `captions:read captions:write`, then provide `WORKOS_*` via the deployment secret store (never `.env`/repo) and smoke `pnpm mcp:conformance` against staging. |
| 3 | **Predefined OAuth client for private beta** (ChatGPT / Claude / Codex testing) | Requires the WorkOS tenant above plus the clients' redirect URIs. | Register one AuthKit OAuth client per tester surface; keep CIMD/DCR disabled until directory readiness. |
| 4 | **Dedicated GCP project + production deployment** | No dedicated GCP project, WorkOS tenant, secret versions, or approved live R2 bucket/token exists. The async `DataStore` cutover is complete: serialized SQLite remains local/test, while pooled PostgreSQL uses connection-pinned transactions, guarded updates, row locks, and `FOR UPDATE SKIP LOCKED`. Its 14-case PostgreSQL 17 concurrency suite passed twice consecutively on 2026-08-29, and the config gate was removed. `deploy_services=false` remains the Terraform default. | Create the dedicated AppAgentic GCP project, decide/approve R2 live provisioning, inject WorkOS/R2/database secrets through Secret Manager, add/verify the final API and web Cloud Run resources, and run `terraform plan` with immutable image SHAs. Enable `deploy_services` only after plan review. Benchmark render CPU/RSS at 2/4/8 vCPU before raising worker concurrency above 1. |
| 6 | **ChatGPT / directory submission** (Phase 4) | Submission is an explicit approval gate. Readiness artifacts live in `docs/directory/` (capability manifest, listing copy, reviewer fixture, starter prompts, submission checklist). CIMD/DCR is a WorkOS setting, not code. | After gates 1–3 pass on staging: enable DCR/CIMD on the WorkOS client, publish `https://clipsubtitles.com/llms.txt`, run the reviewer fixture end to end, then submit through the directory console. |
| 7 | **DNS + TLS for `clipsubtitles.com` / `api.clipsubtitles.com`** | `clipsubtitles.com` was registered on 2026-08-29 with auto-renew, registrar lock, and privacy redaction. DNS and Cloud Run certificates remain unchanged. | After staging services pass, point the site/API records at their verified service endpoints and set `API_PUBLIC_URL`/`WEB_PUBLIC_URL` accordingly (signed URLs and OAuth metadata derive from them). |
| 8 | **Credit purchases / real billing** | v1 grants beta credits on first sign-in (`INITIAL_CREDIT_GRANT`). Purchasing credits needs a payment provider decision. | Decide the provider; add a `grant` ledger entry from a verified webhook (the ledger already enforces idempotent grants). |

## Residual notes (not gates)

- Retired gate 5 is resolved locally: Chrome Headless Shell was installed into the git-ignored Remotion cache and `pnpm smoke:render:remotion` passed (360×640 preview MP4, ProRes 4444 overlay MOV, and SRT). The default production renderer is cropped-band Skia + FFmpeg, whose exact linux/amd64 image canary now verifies FFmpeg/ffprobe, x264/ProRes, Skia PNG output, writable scratch space, and all six bundled Inter font files. Remotion remains optional and is not included in the economical default runtime image.
- A Discord worker-status post was attempted once at the start of this job (per machine-level defaults) and returned `HTTP 401 … FAILED to create worker status thread`; per your instruction no further posts or corrections were sent. It is very likely nothing was posted, but that could not be verified.
- The Chrome MCP tab in this session rendered an error page for `localhost`/`127.0.0.1` (extension-side), so the visual pass relied on your agent-browser findings, the production build, and the API/MCP conformance suites.
